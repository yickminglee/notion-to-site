{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "_comment": "Install only — Nixpacks runs `npm ci` in its own install phase. Adding it here too makes the second npm ci try to remove node_modules while Railway has /app/node_modules/.cache mounted as a build cache, which fails with EBUSY.",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
